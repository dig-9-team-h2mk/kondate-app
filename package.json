{
  "name": "kondate-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:roll": "knex migrate:rollback --all",
    "db:seed-make": "knex seed:make",
    "db:seed": "knex seed:run",
    "build": "npm i && npm run db:migrate && cd client && npm install && npm run build && rm -rf ../public && cp -r dist ../public",
    "db:migrate": "npx knex migrate:latest",
    "db:migrate-make": "knex knex migrate:make ",
    "db:reset": "knex migrate:rollback --all && knex migrate:latest && knex seed:run"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dig-9-team-h2mk/kondate-app.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dig-9-team-h2mk/kondate-app/issues"
  },
  "homepage": "https://github.com/dig-9-team-h2mk/kondate-app#readme",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "firebase-admin": "^13.6.0",
    "knex": "^3.1.0",
    "node": "^20.19.6",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
